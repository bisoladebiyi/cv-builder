import React from 'react'
import styles from "./Navbar.module.css"

const Navbar = () => {
  return (
    <nav className={styles.nav}>
      <p className={styles.logo}>resume<span className={styles.span}>Builder</span></p>
      <div>

      </div>
    </nav>
  )
}

export default Navbar